# Packet Tracer limitations

Packet Tracer is useful for learning and visual simulation, but external SSH automation can be limited depending on version and setup.

The project starts with offline parsing so the validator can be developed and tested even if Packet Tracer does not expose reliable SSH connectivity.
